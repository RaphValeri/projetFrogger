package gameCommons;
import graphicalElements.App;
import javafx.application.Application;

/**
 * Classe permettant le lancement de l'application du jeu Frogger
 */
public class Main {
    public static void main(String[] args) {
        Application.launch(App.class, args);
        }
}
